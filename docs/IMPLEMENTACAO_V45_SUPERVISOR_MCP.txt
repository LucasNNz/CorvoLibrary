CORVO LIBRARY V45 — IMPLEMENTACAO SUPERVISOR CHATGPT VIA MCP
Data: 2026-08-27
Base: V44 LLAMA AUTORIZADO

ARQUITETURA
- APP = executor, persistencia, coleta, materializacao, QA tecnico, metricas e ZIP.
- MCP = estado, evidencias e controles.
- ChatGPT = Supervisor operacional/visual/adaptativo.
- Cloud AI legado = fora do caminho principal; endpoint exige fallback explicito.

IMPLEMENTADO
1. Toggle Supervisor IA LIGADO/DESLIGADO sem chamada a modelo.
2. UI principal sem seleção Qwen/Llama/provider/Neurons/licença.
3. Coleta deterministica continua com Supervisor desligado.
4. Modo noturno persistido em collection_batches.night_mode.
5. PARA_ANALISE somente apos materializacao tecnica real.
6. QA visual automatico interno removido do projeto automatico.
7. SUPERVISOR_DECISION_QUEUE persistente.
8. Tipos QA_VISUAL, RELINK, SOURCE_FAILURE, MATERIALIZATION_FAILURE, TECHNICAL_FIX, CIRCUIT_BREAKER e STRATEGY_REVIEW.
9. obter_estado_supervisor / obter_painel_supervisor.
10. obter_candidatas_qa_visual com arquivo real como MCP resource_link.
11. aprovar/rejeitar/relinkar, pausar/retomar/cancelar projeto e item.
12. alterar referencia/query/fonte/timeout/limites/prioridade/host/configuracao do coletor.
13. atualizar/criar fonte com endpoint, headers tecnicos, user-agent e timeout; headers secretos bloqueados.
14. SOURCE_PROFILE persistente, ativar/desativar, atualizar e SALVAR COMO PADRAO.
15. Perfis iniciais FANDOM_WIKIA_ANIME, YOUTUBE_CONTEXTUAL, ZEROCHAN_ANIME e PINTEREST_DISCOVERY.
16. Metricas tecnicas por fonte alimentam SOURCE_PROFILE; QA do MCP alimenta aprovacao visual.
17. Ranking deterministico de perfil considera prioridade + taxas tecnica/visual.
18. Circuit breaker por host existente + controle MCP manual e fila de revisao.
19. Resumo noturno com cobertura, gaps, URLs, materializados, PARA_ANALISE, falhas, hosts e fontes.
20. Aprovados congelados; projeto concluido manualmente permanece bloqueado.
21. ZIP final usa somente assets resolvidos/aprovados e passa gate de consistencia.
22. Biblioteca madura: somente >=5 variacoes previamente aprovadas entra como fonte principal.
23. Nao repetir candidata rejeitada; coleta volta a buscar outra rota.
24. 3a correcao tecnica exige reavaliacao explicita; correcoes continuam limitadas a ISOLATED e operacoes tecnicas permitidas.
25. Paralelismo global persistente e aplicado ao materializador dentro do teto tecnico seguro.
26. Auditoria de alteracoes do Supervisor em supervisor_config_events.
27. Instrucoes MCP V4 atualizadas com regra de ouro: nao gerar/compor imagem como fallback.

MIGRACAO D1
Arquivo: drizzle/0009_supervisor_mcp_night_collection.sql
Adiciona:
- source_profiles
- supervisor_decision_queue
- supervisor_config_events
- collection_batches.night_mode
- collection_sources.headers_json
- collection_sources.user_agent
- collection_sources.timeout_ms
- settings iniciais do Supervisor MCP/coleta

FLUXO PRINCIPAL
NOITE:
SCRIPT+REQUIREMENTS -> biblioteca madura ou busca externa -> URL -> materializacao -> QA tecnico -> PARA_ANALISE -> fila Supervisor.

MANHA / SUPERVISAO:
obter_resumo_noturno -> obter_estado_supervisor -> obter_candidatas_qa_visual -> APROVAR/REJEITAR/RELINK -> congelar aprovados -> ajustar perfil/fonte/query/host/timeout -> continuar somente gaps -> validar -> gerar ZIP.

SEGURANCA
- Nenhum token/provider de IA aparece no fluxo principal da UI.
- Cloud AI legado exige allow_cloud_ai_fallback=true na rota dedicada.
- Authorization, Cookie, Proxy-Authorization e X-API-Key não podem ser persistidos como headers livres de fonte.
- Exclusoes permanentes existentes continuam exigindo confirmacao.
- Aprovados e projetos concluidos ficam protegidos contra reabertura automatica.

VALIDACAO LOCAL FINAL
Data: 2026-08-27
- 39 arquivos TS/TSX analisados pelo parser do TypeScript: 0 erros sintaticos.
- Migracoes 0000 ate 0009 executadas sequencialmente em SQLite limpo: OK.
- Tabelas V45 confirmadas: source_profiles, supervisor_decision_queue, supervisor_config_events.
- 4 SOURCE_PROFILE iniciais inseridos pela migracao: OK.
- MCP: 127 ferramentas unicas e 127 handlers unicos; 0 ferramentas sem handler e 0 handlers orfaos.
- Anchors dos 21 criterios de aceite conferidos no codigo e no schema.
- Build completo nao foi executado porque a instalacao CI do vinext foi bloqueada por DNS do ambiente (registry.npmjs.org nao resolvido). O bloqueio ocorreu antes da compilacao e nao constitui erro de fonte detectado.
