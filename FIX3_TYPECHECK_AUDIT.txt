CORVO LIBRARY V61.9 — FIX3 TYPECHECK AUDIT

Correcoes adicionais apos os builds reais da Vercel:
1. parseSources: flatMap tipado explicitamente como SourceInput[]; thumbnailPath permanece opcional.
2. materializer: inspectBytes agora retorna uniao discriminada InspectionResult; eliminado ValidInspection=never.
3. bootstrap Turso: ensureBootstrapSettingsTable possui retorno Promise<void> nao-nulo explicito.
4. replanFailedItem: removidas comparacoes impossiveis com TRY_NEXT_SOURCE no planner deterministico.
5. MCP FAST PUSH: batch_id null e normalizado para undefined onde a assinatura exige opcional.
6. QA packet: candidatos mantem tipo Record<string,unknown> apos spread.
7. package: Node runtime 24.x alinhado com @types/node 24.13.3.
8. build Vercel: npm run typecheck executa antes de next build para listar erros TypeScript em lote.
9. dependencias criticas foram fixadas em versoes exatas para reduzir drift entre deploys.

Validacoes locais possiveis neste ambiente:
- npm test: 135/135 PASS
- validate-vercel-migration: PASS
- 86 arquivos TS/TSX: 0 erros sintaticos via TypeScript parser
- ZIP integrity: verificar no empacotamento

Limitacao:
O ambiente local nao possui acesso DNS ao registry npm; portanto, o typecheck completo com os tipos reais de Next/React/Drizzle/AWS/libSQL ainda precisa rodar no build Vercel. A FIX3 foi configurada para executar `tsc --noEmit --pretty false` antes do `next build`, de modo que futuros erros sejam exibidos em lote.
