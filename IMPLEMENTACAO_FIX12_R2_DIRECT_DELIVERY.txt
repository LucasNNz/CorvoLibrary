CORVO LIBRARY V61.9 — FIX12 R2 DIRECT DELIVERY

Problema confirmado:
- catálogo/Turso restaurado corretamente;
- objeto AST-E4E00865DC43ADF00746 confirmado como R2_DIRETO no bucket externo;
- UI Vercel falhava porque /api/files/:id fazia proxy/stream de bytes via adaptador env.BUCKET.

Correção:
- /api/files/:id continua validando owner/MCP e expiração;
- depois gera URL GET assinada do R2 externo e responde 307;
- preview usa URL inline sem Content-Disposition attachment;
- download usa filename original;
- bytes não atravessam Vercel;
- erros agora distinguem R2_OBJECT_NOT_FOUND e R2_SIGNED_URL_FAILED.

R2 e Turso não são alterados por esta correção.
