CORVO LIBRARY V61.9 — FIX5 DRIZZLE TYPE SWEEP

Correções motivadas pelo type-check real da Vercel:

1. performance-control.ts
- supervisor_project_candidates não possui mime_type/width/height.
- O snapshot operacional agora faz LEFT JOIN com materialization_files por materialization_file_id.
- mime_type, width e height são lidos da tabela canônica correta.

2. worker-orchestration.ts
- Removido array heterogêneo de statements com cast entre updates de worker_work_items/worker_sessions/export_jobs.
- db.batch recebe tuplas literais concretas para o caso normal e EXPORT_JOB.

3. fast-push.ts
- sha256/r2Key/mimeType são congelados em constantes não-nulas depois da atualização da candidata.
- Drizzle eq() não recebe mais candidate.sha256 nullable após reatribuição da candidata.

4. supervisor-control.ts
- batches dinâmicos de tabelas diferentes foram separados em batches homogêneos.

Validação local disponível sem node_modules externos:
- suíte regressiva completa: 142/142 PASS
- auditoria schema x referências: nenhuma referência real a coluna inexistente
- regressões específicas FIX5 adicionadas.

O build real da Vercel continua sendo a fonte de verdade para os tipos completos de Drizzle/Next instalados.
